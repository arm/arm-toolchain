#!/usr/bin/make -f

build:

install:

clean:

binary-arch:
	mkdir -p ${DPKG_BINARY_DIR}/DEBIAN
	dpkg-gencontrol -P${DPKG_BINARY_DIR}
	dpkg-deb -b ${DPKG_BINARY_DIR} ..

binary: binary-arch

.PHONY: % build install clean binary-arch binary
